# Term 2, Week 7 — 15 August 2026

**Composition:** *A Surprise* · Draft 1 of 2 · The Write Connection

## Files

| File | What it is |
| --- | --- |
| `a-surprise-draft-two.html` | Tutoring page. Open it in a browser — it is self-contained, with the scans embedded, so it works offline and needs no server. |
| `scans/draft-1-page-1.jpg` | Page 1 of the marked draft, rotated upright. Lines 1–15. |
| `scans/draft-1-page-2.jpg` | Page 2 of the marked draft, rotated upright. Lines 16–29. |

## What the tutoring page covers

- The full draft transcribed line by line, with all **29 red marks** as clickable annotations. Each opens into the code's meaning, why it was marked, a before/after rewrite, a word bank, and a "go further" prompt.
- Margin remarks reproduced in the teacher's hand, also clickable.
- A **Show corrections** toggle that overlays the red insertions inline.
- Per-mark "Fixed in Draft 2" ticks, saved in the reader's browser.
- **Two recommendations** for the rewrite, in the order they should be done:
  1. Rebuild the middle from the narrator's point of view (the `CI` remark).
  2. Give the reveal more room than the rules of the test.
- **The whole composition, put right** — three views: the clean corrected copy, the same text with changes shown, and a model Draft 2 that acts on both recommendations.

## Marks by category

| Code | Count | |
| --- | --- | --- |
| `SP` | 7 | spelling |
| `T` | 4 | tense |
| `PV` | 2 | powerful verbs |
| `G` | 2 | grammar |
| `We` | 2 | pronouns |
| `CI` | 1 | point of view |
| `4Ws` | 1 | missing setting |
| `Show` | 1 | show, not tell |
| ✳ | 2 | done well (SWAB opening, link to topic) |

## Notes

- Line 11's spelling is illegible in the scan — the teacher struck the word through and wrote *stealthily* over it. The page renders the original as *stealthly*; the correction taught is right either way.
- `CI` is glossed as *your character's inside view*, read from the remark "*(CI) point of view*". The page says on its face that the teacher's own classroom wording takes precedence.
- Two words in the clean corrected copy are marked with a dotted green underline. Those are suggestions, not the teacher's: she asked a question there (*what test? · describe the puppy*) instead of supplying the word.

## Published

Live page: <https://claude.ai/code/artifact/89190a0a-e34f-4662-8dbd-021710053f1c>

The artifact is tied to that URL, not to this path. To update it after this move, republish passing the URL explicitly rather than the file path alone, or a second, separate artifact is created.
